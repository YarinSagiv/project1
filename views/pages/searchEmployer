<html>

<head>
  <link href="css/sty.css" rel="stylesheet">
</head>

<body>

  <header>
    <%- include('../partials/_header') %>
  </header>

    <form action="/searchEmployer" method="post">
        <label style="font-size:20px;">
            <h3 class="searchingEmployer" for="LsearchingE:"><b>searching employer: </b></h3>
            <label class="searchingEmployer" for="empID"><b>employer id: </b></label>
            <input type="text" placeholder="Enter employer ID" name="iduser" id="iduser" oninput="this.value = this.value.replace(/[^0-9]/g, '').replace(/(\..?)\../g, '$1');" required>
        </label>
        <input type="submit" name="send" value="search" >
        <input type="reset" name="reset1" value="reset">
    </form>  

    <% if(typeof messageNRE != "undefined" && messageNRE) { %>
    <label class="searchingEmployer" style="display: block; text-align: center;"><%=messageNRE%></label>
    <% } else if(emploerFound && typeof employerFound !="undefined"){ %> 
</body>

</html>